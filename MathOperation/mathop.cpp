#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    
    double x =3.99;
    double y =4;
    double z;

    // z = max(x,y);
    // z = min(x,y);
    // z = pow(x,y);
    // z = sqrt(y);
    // z = abs(-y);
    // z = round(x);
    // z = ceil(x);
    z = floor(x);

    cout<<"Result = "<<z;

    return 0;
}
